# Проверка версии 1.3

Дата: 2026-09-21. Windows x64, .NET SDK 10.0.201, Runtime 10.0.5.

## Поддерживаемая сборка

```text
Steam build: 25382007
Module: Stalker2-Win64-Shipping.exe
Size: 174798384 bytes
SHA-256: 61BC1E030740CEBC30CF1DAD0C86CF65E39E12FF0500225821D684181E08D56B
```

Номер сборки прочитан из установленного Steam-манифеста, SHA-256 вычислен
по установленному EXE. Старый профиль 24963344 заменён новым; прежний
релиз 1.2.0 остаётся доступен для старого EXE. Файлы игры не изменялись.

## Сопоставление адресов

Сигнатуры ранее исследованных функций сопоставлены с новым EXE.
Структуры дополнительно проверены чтением текущего процесса:

| Назначение | Новый RVA |
|---|---|
| Handle персонажа | `0x9EEC140` |
| Пул персонажей | `0xA332940` |
| Таблица модели персонажа | `0x8EB2B10` |
| Массив блоков UObject / количество | `0xA0E68D0` / `0xA0E68E4` |
| Пул предметов | `0xA7859A0` |
| Таблица модели оружия | `0x8EB1390` |
| Таблица модели гранаты | `0x8EAF960` |
| Купоны | `0xA80DA90` |

Проверены full handle, серийный номер UObject, обратная ссылка actor → данные
персонажа, связи предметов с моделями и принадлежность гранат игроку.
Размеры записей пулов остались прежними: персонаж `0x700`, предмет `0x88`.

- Флаги по `player+0x13C`: неуязвимость 1, магазин 4, боезапас 8,
  отключение заклинивания 16.
- Выносливость/максимум: `+0x150/+0x154`, кровотечение `+0x158`,
  радиация `+0x16C`, голод `+0x17C`, сонливость `+0x184`.
- Найдены пять экипированных предметов. Геттеры прочности подтверждены:
  оружие RVA `0x20906F4` (model + `0x98`), броня RVA `0x1AF7E0`
  (model + `0x40`). Удержание не ремонтирует уже повреждённые предметы.
- Данные стрельбы: кэш по `model+0x90`, полезная часть с `+8`.
  Новый геттер RVA `0x617992`; расчёт радиуса RVA `0x61159A`.
  Базовые радиусы по `+0x1F0/+0x1F4/+0x280` сохранились.
  До теста прочитаны 568/568/2200, 124/124/200 и 189/189/300.
- Гранаты: списание RVA `0x300A8D8`, изменение количества RVA `0xC18810`.
  Количество по `item+0x44`; владелец по `item+0x20`, handle владельца
  по `owner+0x68`. Найдены две стопки `GrenadeRGD5`: 2 и 5 штук.
- Таблица модификаторов по `player+0x668` прочитана с проверкой хеш-цепочек;
  модификатор износа `0x7E` до теста равен 3.75 у оружия в руках.
- По новому адресу купонов прочитано **105000**; пользователь подтвердил
  эту же сумму. Купоны при проверке профиля не изменялись.

## Проверки

**34/34 автоматических теста пройдены.** Проверяются числовые форматы,
сканирование, чтение и запись специальных тестовых процессов, отмена,
восстановление флагов и радиусов оружия, пересчёт кэша, смена персонажа,
износ, владелец и поколение гранат, пустые стопки и циклы пула.
Эти тесты не изменяют память игры.

В ограниченном по времени игровом тесте активировались **11 из 11** функций,
защищались пять предметов и две стопки гранат. Контрольные чтения показали
флаги `0x1D`, нулевые голод и радиацию. Отключение и восстановление параметров
выполняются в блоке `finally`.

Пользователь подтвердил работу функций в новом профиле. После завершения
теста повторное чтение показало исходные флаги `0x0` и восстановленные
радиусы оружия 568/568/2200, 124/124/200 и 189/189/300.

## Ограничения

Трейнер поддерживает только указанный SHA-256 EXE. Сценарные смерти,
сонливость отдельным игровым действием и новый перезапуск игры не проверены.
Фиксированный RVA купонов подтверждён для текущего запуска: сверяй сумму
перед записью. Перед загрузкой другого сохранения выключи функции,
после загрузки подключись заново. Пополненные гранаты и снятые состояния
после выключения не откатываются. Аварийное завершение процесса трейнера
не выполняет восстановление флагов и параметров оружия.
